{
  "name": "tasteprint-escape",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:accessibility && npm run test:data && npm run test:distribution",
    "test:accessibility": "node scripts/check-accessibility.js",
    "test:data": "node scripts/check-data-contract.js",
    "test:distribution": "node scripts/simulate.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
